- name: get the password from AWS secrets manager
  debug:
    msg: "{{ lookup('amazon.aws.aws_secret', 'roboshop/mysql_root_password', region='us-east-1') | from_json  }}"

# - name: print root password
#   debug:
#     msg: "{{ root_password.mysql_root_password }}"

- name: install mysql
  ansible.builtin.package:
    name: mysql-server
    state: present
- name: starting mysql
  ansible.builtin.service:
    name: mysqld
    state: started
    enabled: yes
- name: set root passoword
  ansible.builtin.command:
    cmd: mysql_secure_installation --set-root-pass "{{ root_password.mysql_root_password }}" 